# Portfolio Prices JSON

Portfolio App price JSON hosting files.

`prices.json` is generated by:

```bash
python3 tools/generate_prices_json.py
```

GitHub Pages URL example:

```text
https://<username>.github.io/<repository>/prices.json
```
